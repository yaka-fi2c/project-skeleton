module.exports = function (grunt) {
    grunt.initConfig({
        builder: {
            backboneTodoMVC: {
                src: 'index.html',
                options: {
                  dirDest: 'backbone/',
                  appcache: true
                }
            }
        },
    });
    grunt.loadNpmTasks('grunt-builder');

    grunt.registerTask('build', ['builder']);
};